import 'package:flutter/material.dart';

class ChatsListPage extends StatelessWidget {
  const ChatsListPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child:Text('ChatsListPage')),
    );
  }
}